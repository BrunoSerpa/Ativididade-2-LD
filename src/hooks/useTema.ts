import { mega, quina, timemania } from "../styles/theme";

export default function useTema(){
    const Temas = {
        themeMegasena: mega,
        themeQuina: quina,
        themeTimemania: timemania
    }
    return Temas;
}